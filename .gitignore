# Config local de Claude Code (no publicar)
.claude/

# Material fuente pesado / no usado por la app
Body/
a/

# Sistema
Thumbs.db
.DS_Store

# Worker de Cloudflare (camara IA) — dependencias locales, no se publican
worker/node_modules/
worker/.wrangler/
